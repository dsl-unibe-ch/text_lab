__pycache__
mcp_artifacts/
# Built apptainer images and runtime logs live on research storage, not in git.
*.sif
logs/
.streamlit/
bin/
etc/
lib/
lib64
pyvenv.cfg
share/
todo

# Vendored test-only wheels (numpy/cv2/lxml) pulled in to run tests headlessly.
.testdeps/

# Working scratch: experiment write-ups, handoffs, proposed plans, and the
# one-off audit harnesses. Kept on disk for reference, deliberately out of the
# repo, which holds code only.
notes/